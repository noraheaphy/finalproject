---
title: "ancestral_reconstruct"
author: "Nora Heaphy"
date: "11/15/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(ape)
library(ggtree)
library(phytools)
library(tidyverse)
library(ggplot2)
library(here)

setwd(here())

```

# Ancestral Reconstruction

## Read in data

```{r}

# read in tree file
portulacaTree = read.tree("Ocampo_Portulaca.parts.treefile")

tip_lab <- data.frame(portulacaTree$tip.label) %>%
  rename(tiplab = portulacaTree.tip.label) %>%
  mutate(tiplab_renamed = gsub(, tiplab))



```
